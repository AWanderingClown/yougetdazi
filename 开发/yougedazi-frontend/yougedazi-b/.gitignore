# 系统文件（影响所有子项目）
.ini
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE 配置（影响所有子项目）
.idea/
.vscode/
*.swp
*.swo
*~

# 临时/备份文件（影响所有子项目）
tmp/
temp/
*.tmp
*.bak
*.backup
.cache/

# 日志（影响所有子项目）
*.log
logs/

# 微信小程序（影响所有子项目）
project.private.config.json

# Node.js （影响所有子项目）
node_modules/
npm-debug.log*
yarn-error.log*

# 环境变量（影响所有子项目）
.env
.env.local
.env.*.local
.env.example  # 如果包含真实数据也要忽略，只保留 env.example.template

# 编译输出（影响所有子项目）
dist/
build/
coverage/

# 其他（影响所有子项目）
*.tmp
.cache/# 系统文件（影响所有子项目）
.ini
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE 配置（影响所有子项目）
.idea/
.vscode/
*.swp
*.swo
*~

# 临时/备份文件（影响所有子项目）
tmp/
temp/
*.tmp
*.bak
*.backup
.cache/

# 日志（影响所有子项目）
*.log
logs/

# 微信小程序（影响所有子项目）
project.private.config.json

# Node.js （影响所有子项目）
node_modules/
npm-debug.log*
yarn-error.log*

# 环境变量（影响所有子项目）
.env
.env.local
.env.*.local
.env.example  # 如果包含真实数据也要忽略，只保留 env.example.template

# 编译输出（影响所有子项目）
dist/
build/
coverage/

# 其他（影响所有子项目）
*.tmp
.cache/